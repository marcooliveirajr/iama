<%= form_for(@output, html: { class: "ls-form row" }) do |f| %>
  <fieldset>

    <div class="row">
      <div class="col-md-8">
        <label class="ls-label col-md-8 <%= class_error @output, :category_id %>">
        <span class="ls-label-text"><%= f.label :category_id, "Categoria:" %></span>
          <div class="ls-custom-select">
            <%= f.collection_select(:category_id, outputs_filter, :id, :name, class: "ls-state ls-select", prompt: true) %>
          </div>
          <%= show_message_error @output, :category_id %>
        </label>
      </div>
    </div>

    <div class="row">
      <div class="col-md-8">
        <label class="ls-label col-md-8 <%= class_error @output, :name %>">
        <span class="ls-label-text"><%= f.label :name, "Nome:" %></span>
        <%= f.text_field :name %>
        <%= show_message_error @output, :name %>
      </label>
     </div>
    </div>

 </fieldset>

  <div class="ls-actions-btn">
      <%= f.button  "Salvar", :type => "submit" , class: "ls-btn-primary"%>
      <%= link_to 'Voltar', outputs_path, class: "ls-btn-darker" %>
  </div>

<% end %>


