<h1 class="ls-title-intro ls-ico-text">Quartos</h1>
<%= flash_messages %>
<a><%= link_to 'Cadastrar Quarto', new_bedroom_path(@bedroom) , class: "ls-btn-primary" %></a>

<br></br>

<tbody>
  <% @bedrooms.each do |bedroom| %>

  <div class="ls-list">
    <header class="ls-list-header">
      <div class="ls-list-title col-md-9">
        <strong><%= link_to bedroom.name, bedroom %></strong>
      </div>
      <div class="col-md-3 ls-txt-right">
        <div data-ls-module="dropdown" class="ls-dropdown ls-pos-right">
          <a href="#" class="ls-btn-primary">Administrar</a>
          <ul class="ls-dropdown-nav">
            <li><%= link_to 'Editar', edit_bedroom_path(bedroom) %></li>
            <li><%= link_to 'Visualizar', bedroom %></li>
            <li><%= link_to 'Excluir', bedroom_path(bedroom), data: { confirm: "Você tem certeza?" }, method: :delete, class: "ls-color-danger ls-divider" %></li>
          </ul>
        </div>
      </div>
    </header>
  </div>

  <% end %>
</tbody>
