<h1 class="ls-title-intro ls-ico-text">Plantonistas</h1>
<%= flash_messages %>
<tbody>
  <% @on_duties.order("on_duty_date asc").each do |on_duty| %>

  <div class="ls-list">
    <header class="ls-list-header">
      <div class="ls-list-title col-md-9">
        <span class="ls-list-label">Data / Anesteista</span>
        <strong><%= on_duty.on_duty_date.strftime("%d/%m/%Y") %> / <%= on_duty.anesthetist.try(:name) %></strong>
      </div>
    </header>
    <div class="ls-list-content ">
      <div class="col-xs-12 col-md-6">
        <span class="ls-list-label">Telefone</span>
        <strong><%= on_duty.anesthetist.try(:phone) %></strong>
      </div>
      <div class="col-xs-12 col-md-6">
        <span class="ls-list-label">Email</span>
        <strong><%= on_duty.anesthetist.try(:email) %></strong>
      </div>
    </div>
  </div>

  <% end %>
</tbody>
